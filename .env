
# Frontend config (used by Vite)
VITE_API_URL=http://localhost:3001/api

# Note: The backend reads env from `backend/.env` when started from the backend/ folder.
# The backend .env is created at `backend/.env` and contains DB and JWT settings for local development.